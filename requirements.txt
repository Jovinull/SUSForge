# =====================================================================
# SUSForge — Dependências de runtime
# ---------------------------------------------------------------------
# Camada de orquestração + ETL local. Os contêineres do Airflow trazem
# `apache-airflow` pré-instalado; aqui ele entra para suportar
# importação/lint de DAGs no host (IDE, mypy, testes).
#
# Instalação:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -U pip wheel
#   pip install -r requirements.txt
#
# Para Airflow respeitar suas constraints oficiais (OBRIGATÓRIO):
#   AIRFLOW_VERSION=3.2.2
#   PYTHON_VERSION=3.12
#   CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"
#   pip install --constraint "${CONSTRAINT_URL}" -r requirements.txt
# =====================================================================

# Orquestração — Airflow 3.2.x: SQLAlchemy 2 + Flask 3 + cryptography
# moderna. As séries 3.0.x e 3.1.0–3.1.7 ainda pinam SQLAlchemy 1.4,
# por isso fixamos em 3.2.x para de fato eliminar a dívida técnica.
# Pin EXATO para casar com a imagem no docker-compose.yml.
apache-airflow==3.2.2
# FAB Auth Manager — preserva fluxo `airflow users create` e admin via env
apache-airflow-providers-fab

# Processamento colunar (Polars é o padrão; Pandas é proibido no caminho quente)
polars>=1.0.0

# Validação de dataframes e schemas declarativos
pandera>=0.20.0

# Modelos tipados e configuração via env
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP para downloads do OpenDATASUS
requests>=2.31.0

# Camada de banco — SQLAlchemy 2.x liberado pelo Airflow 3
SQLAlchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.9

# CLI do projeto (susforge ...)
typer>=0.12.0
