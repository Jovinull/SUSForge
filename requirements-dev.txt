# =====================================================================
# SUSForge — Dependências de desenvolvimento
# ---------------------------------------------------------------------
# Lint, tipagem, testes e hooks de pré-commit.
#
# Instalação:
#   pip install -r requirements-dev.txt
# =====================================================================

-r requirements.txt

# Lint + formatação (regras E, F, I configuradas em pyproject.toml)
ruff>=0.6.0

# Tipagem estática estrita (alvo mypy --strict)
mypy>=1.10.0

# Testes
pytest>=8.0.0
pytest-cov>=5.0.0

# Hooks (ruff + mypy + checks básicos antes do commit)
pre-commit>=3.7.0
